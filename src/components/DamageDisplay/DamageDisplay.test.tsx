import React from 'react';
import { render, screen } from '@testing-library/react';
import { DamageDisplay } from './DamageDisplay'

test('renders learn react link', () => {
  render(<DamageDisplay typesArray={['water','rock']} />);

  
  
});
